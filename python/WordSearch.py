class WordSearch:
    def exist(self, board, word):
        NX = len(board)
        NY = len(board[0])
        for x in range(NX):
            if board[x][0] == word[0]:



    def dfs(self, board, x, y, )